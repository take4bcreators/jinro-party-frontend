/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // 厳格モード (2回レンダリングをやめる場合は false にする)
  //   output: 'export', // 静的サイトビルドの場合は指定する
};

module.exports = nextConfig;
